## [1.0.7] - 2026-08-12

### Added

- SFTP 连接增加 SHA-256 主机指纹校验和首次信任确认，实际上传、比较、删除、回滚与远程命令均不再静默接受未知主机
- AI 对话、图像生成与本地数据管理关键路由增加真实 Electron 冒烟覆盖
- AI 对话增加受限本机历史暂存与 Markdown 导出，异常或超量历史会在载入、追加与保存时安全收敛
- AI Provider 增加默认关闭的自动故障转移、本机回环优先、1–3 次尝试、失败冷却和安全路由观测；仅使用已添加且最近测试通过的 Provider，流式输出开始后与用户取消时不会切换
- AI 对话升级为本机多会话管理，最多保留 24 个；旧 `aiChatHistory` 会自动迁移，并支持搜索、重命名、删除以及 MD/JSON 导出；本地暂存不进入加密备份，持久化和导出会脱敏常见 API Key、Token、密码及授权字段
- AI 对话增加 OpenAI Chat/Responses、Anthropic 与 Gemini 的流式输出、停止生成、部分回答保留和手动重试；流式增量在进入 Renderer 前完成跨分片敏感信息保护
- AI 对话支持最多 4 张受控故障截图证据；系统文件选择器仅接收 PNG、JPG/JPEG、WEBP，主进程按 Renderer 隔离完成解码、限额、缩放和规范化，Renderer 仅持有临时标识与受限预览
- OpenAI Chat/Responses、Anthropic 与 Gemini 的对话请求统一支持受控图片证据；多模态调用不记录图片 Base64，并按未知费用进入保护性预算校验
- AI 生图远程图片下载增加协议、端口、DNS 地址、逐跳重定向和响应体大小校验，阻止访问本机、内网、链路本地及保留地址
- AI 生图默认使用 `gpt-image-2`，支持 1–4 张批量、基于本地生成图的编辑/变体、在途取消、临时错误自动重试和原消息手动重试
- AI 生图增加下一批费用估算、兼容 Provider 自定义每张费用、单批费用上限，以及与 AI 对话共用的每日/月度预算保护；超限时使用绑定 Renderer 和请求参数的一次性确认

### Changed

- AI 生图原图改为独立保存在受限本地资源目录；历史 JSON 只保留小尺寸预览和资源标识，并设置 80 条、单图 50 MB、预览 48 KB、资源总量 512 MB 的上限
- AI 用量中心统一统计对话与生图请求、Token、图片张数、费用来源和估算费用；预算按本机日期汇总
- 剪贴板历史增加 200 条、文本单项 1 MiB、图片单项 5 MiB、总持久化 20 MiB 的限制；读取旧数据和写入 IPC 时都会重新校验并安全截断
- 版本号升级到 1.0.7；GitHub Release 继续发布带 SHA-256 校验文件的 macOS arm64/x64 与 Windows x64/arm64 产物

### Fixed

- 修复完整 Base64 图片直接写入 `gpt-image-history.json` 导致历史文件快速膨胀和启动内存压力的问题
- 修复 AI 图片保存可通过远程 URL 请求本机或内网资源的 SSRF 风险
- 修复 SFTP/SSH 默认自动接受任意服务器主机密钥导致的中间人攻击风险
- 修复超大或畸形剪贴板记录可绕过渲染层直接写入本地历史的问题
- 修复 AI 对话本机存储配额或策略异常会阻断继续提问的问题
- 修复长耗时 AI 请求只能等待固定超时、无法主动停止，以及部分 Responses 兼容服务仅在完成事件返回正文时被误判为空响应的问题
- 修复 AI 生图历史清理失败时界面仍误报已清空，以及内部工作区样式命名与全局页面框架冲突的问题
- 修复同一生图请求标识并发执行时旧请求可能释放新请求预算预留，以及用量持久化失败后预算占用被静默释放的问题
- 修复 AI 对话图片证据的重复导入、单项移除、成功发送清理和 Provider 接口回退可能出现状态不一致的问题

### Security

- macOS 产物当前仍未使用 Apple Developer ID 签名，也未提交 Apple 公证；首次打开可能需要用户在系统设置中确认，发布页会继续明确披露这一限制

## [1.0.6] - 2026-08-10

### Added

- 统一任务中心的维护窗口、自动化批量执行、操作审计管理和 Windows Node 进程指标闭环
- 从 GitHub 私有仓库 Release 检查、下载并校验当前平台在线更新包

### Changed

- Tag 发布流水线改为直接向草稿 Release 上传 macOS/Windows 资产，并汇总 `SHA256SUMS.txt`，不再占用 Actions artifact 配额
- 发布产物继续保持未使用 Apple Developer ID 签名和公证的 macOS arm64/x64 DMG/ZIP

### Fixed

- 维护窗口启用、缩短、延长或关闭后会立即重排自动备份，避免继续等待旧的恢复时间

## [1.0.5] - 2026-08-07

### Added

- 新增基于 GitHub Release 的应用更新检查、下载、SHA-256 校验和安装引导

## [1.0.4] - 2026-08-07

### Changed

- GitHub Release 改为发布未使用 Apple Developer ID 签名和公证的 macOS arm64/x64 安装包
- Release 页面会明确提示 macOS 产物未签名，首次打开时可能出现系统安全提示

## [1.0.3] - 2026-08-06

### Fixed

- 修复 base.css 中重复的全局按钮样式块（重复代码导致的潜在维护问题）

## [1.0.2] - 2026-08-06

### Fixed

- 之前发布的 base.css 重复样式块已清理

## [1.0.1] - 2026-08-02

### Added

- 完整发布 Checklist
- Windows / Linux 安装说明

### Changed

- 版本号升级到 1.0.1

## [1.0.0] - 2026-07-31

### Added

- 核心运维功能（系统发布、模型可靠性、Node 服务）
- AI 能力中心
- 本机工具（快捷启动、剪贴板、备份恢复）
- 严格的安全沙箱模型

### Changed

- 架构文档完善

### Fixed

- 核心安全闭环与 IPC 边界
- 测试用例覆盖 99 项

## [0.1.0] - 2026-07-28

### Added

- Electron + Vue 3 + Vite 基础框架
- 初始版本发布
