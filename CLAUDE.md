# Project Instructions

For any renderer page, layout, card, form, table, or responsive-style work, read and follow the shared project skill at `.claude/skills/app-page-layout/SKILL.md` before making changes.

`.claude/skills/app-page-layout` is a tracked symlink to `.codex/skills/app-page-layout`, so Claude and Codex use exactly the same layout contract. Do not replace it with a copied second version.
